import MarketingHeader from "@/components/MarketingHeader";

export const config = {
  runtime: "edge",
};

const Page = () => {
  return (
    <>
      <MarketingHeader />
    </>
  );
};

export default Page;
